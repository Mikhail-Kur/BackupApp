in the "settings" file, specify the path to the folder 
from which you want to create a backup in the "UrlOriginal" 
column and the path to the folder to which the backup will be created 
in the "UrlCopy" column. You can also set the logging level when executing 
the program in the "actionOption" column: 1 "information" 2 "debugging" 3 "error"